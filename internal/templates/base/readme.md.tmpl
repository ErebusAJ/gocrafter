# {{.ProjectName}}

A backend project scaffold created using **Gocrafter** with essential add-ons to kickstart your Go development.

## Features
- Go-based backend template
- Pre-configured for RESTful APIs
- Includes essential add-ons (e.g., logging, database setup, etc.)

## Installation

1. Install the scaffold package:

    ```bash
    go install github.com/ErebusAJ/gocrafter@latest
    ```

2. Verify the installation:

    ```bash
    gocrafter --version
    ```

## Usage

Generate a new Go backend project:

```bash
gocrafter init -t <template-name> --name <project-name>
